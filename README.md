# Backend user authentication api with confirmation email



## Create .env file and add this details. Change the credentials accordingly
MONGO_URI = mongodb+srv://<username>:<password>@cluster0.rmfcw.mongodb.net
NODEMAILER_EMAIL_USERNAME = youemail@gmail.com
NODEMAILER_EMAIL_PASSWORD = yourpassword
NODEMAILER_SMTP_ENDPOINT = smtp.gmail.com
NODEMAILER_SMTP_PORT = 587
JWT_SECRET = s3cret
FRONTEND_BASE_URL = localhost:3000

## Postman Collection included 
Contact API
